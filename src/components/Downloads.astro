---
import { downloads } from '../content/downloads.js';
---
<head>
    <link rel="stylesheet" href="/src/styles/Downloads.css">
</head>
<section class="downloads-section">
    <div class="downloads-grid">
        {downloads.map((download) => (
        <div class="download-card">
            <h3 class="download-name">{download.name}</h3>
            <p class="download-description">{download.description}</p>
            <p class="download-info">Version: {download.version}</p>
            <p class="download-info">Release Date: {download.releaseDate}</p>
            <p class="download-info">Size: {download.size}</p>
            <p class="download-info">Checksum: {download.checksum}</p>
            <a href={download.magnetUrl} class="download-button" download>Magnet 🧲</a>
            <a href={download.torrentUrl} class="download-button" download>Torrent 📥</a>
            <a href={download.mirrorUrl} class="download-button" download>Mirror 💾</a>
        </div>
        ))}
    </div>
</section>

